package br.edu.infnet.ecommerce.pagamento.domain;

/** Substitui as strings de status que o monólito espalhava pelos services. */
public enum StatusPagamento {
    PENDENTE,
    APROVADO,
    RECUSADO
}
